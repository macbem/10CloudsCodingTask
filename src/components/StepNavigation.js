import React from 'react';
import styled from 'styled-components';

const StepNavigation = props => (
  <StepNavigationComponent>
    {/* implement a styled component here */}
    <nav>
      <a href="#" />
      <a href="#" />
      <a href="#" />
    </nav>
  </StepNavigationComponent>
);

export default sidePanel;
